info:
  name: shortcut_2
  type: http
  seq: 20

http:
  method: get
  url: https://malavika16.erpgulf.com:3045/api/method/employee_app.attendance_api.get_shortcut_2
  body:
    type: form-urlencoded
    data:
      - name: employee
        value: HR-EMP-00003
  auth:
    type: bearer
    token: uxWOuJdRomYHGqbksQZqoOzI1jMCiX

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |2-
   # Get Shortcut 2

    Returns the data for the second configurable dashboard shortcut for an employee.

    ## Authentication
    Requires a valid Bearer token.

    ## Parameters

    | Name     | Type   | Required | Description                         |
    |----------|--------|----------|-------------------------------------|
    | employee | string | Yes      | Employee ID (e.g., `HR-EMP-00003`) |

    ## Response

    ```json
    {
      "message": {
        "label": "Designation",
        "value": "Software Engineer"
      }
    }
    ```

    ## Notes
    - See `get_shortcut_1` for configuration details.
    - The field displayed is configured per-company in `Checkin App Setting`.
