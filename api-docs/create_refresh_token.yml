info:
  name: create_refresh_token
  type: http
  seq: 11

http:
  method: GET
  url: https://malavika16.erpgulf.com:3045//api/method/employee_app.gauth.create_refresh_token
  headers:
    - name: Content-Type
      value: application/x-www-form-urlencoded
    - name: Authorization
      value: Bearer Pk9q9SSFsIKgEtD3CCY7YWCtU6MbUf
      disabled: true
    - name: Cookie
      value: full_name=aysha%20p; sid=Guest; system_user=no; user_id=Guest; user_image=
  body:
    type: form-urlencoded
    data:
      - name: refresh_token
        value: fUkfDAWMRZXIcl81aOwU1eCtnIFZ1P

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |2-
   # Create Refresh Token

    Exchanges a valid refresh token for a new access token.
    This is a **guest endpoint** — no Bearer token required.

    ## Parameters

    | Name          | Type   | Required | Description                                         |
    |---------------|--------|----------|-----------------------------------------------------|
    | refresh_token | string | Yes      | The refresh token received from `generate_token_secure` |

    ## Response

    ```json
    {
      "message": {
        "access_token": "<new_bearer_token>",
        "expires_in": 3600,
        "token_type": "Bearer",
        "scope": "all",
        "refresh_token": "<new_refresh_token>"
      }
    }
    ```

    ## Notes
    - Call this endpoint when the current access token expires to get a new one without re-entering credentials.
    - The old refresh token is invalidated after use; store the new one returned in the response.
