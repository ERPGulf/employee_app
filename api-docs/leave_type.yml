info:
  name: leave_type
  type: http
  seq: 15

http:
  method: get
  url: https://malavika16.erpgulf.com:3045/api/method/employee_app.attendance_api.get_leave_type
  headers:
    - name: Content-Type
      value: application/x-www-form-urlencoded
    - name: Authorization
      value: Bearer hDtBIPZHOpnkqvZ5dmr7KIJ1zHwkxL
  body:
    type: form-urlencoded
    data:
      - name: employee
        value: HR-EMP-00003
  auth:
    type: bearer
    token: c7854u0Hy4weGC66HtgBQzafNF9sRv

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |-
  # Get Leave Types

    Returns the list of available leave types for an employee, including their
    remaining balance and allocation details.

    ## Authentication
    Requires a valid Bearer token.

    ## Parameters

    | Name     | Type   | Required | Description                         |
    |----------|--------|----------|-------------------------------------|
    | employee | string | Yes      | Employee ID (e.g., `HR-EMP-00003`) |

    ## Response

    ```json
    {
      "message": [
        {
          "leave_type": "Annual Leave",
          "max_days_allowed": 30,
          "remaining_balance": 12.0
        },
        {
          "leave_type": "Sick Leave",
          "max_days_allowed": 15,
          "remaining_balance": 15.0
        }
      ]
    }
    ```

    ## Notes
    - Returns only leave types with an active allocation for the employee's current leave period.
    - Use the `leave_type` name from this response when calling `create_leave_application`.
