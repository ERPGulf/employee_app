info:
  name: create complaint
  type: http
  seq: 23

http:
  method: post
  url: https://malavika16.erpgulf.com:3045/api/method/employee_app.attendance_api.create_complaint
  headers:
    - name: Content-Type
      value: application/x-www-form-urlencoded
    - name: Authorization
      value: Bearer QEj2moTEdoZ79s6ZKt5B9jOnYC4fya
  body:
    type: form-urlencoded
    data:
      - name: employee
        value: HR-EMP-00003
      - name: date
        value: 2026-06-01
      - name: message
        value: testing
  auth:
    type: bearer
    token: mLzKQMuWeVYJV4NGBPVsU69yP3dvQa

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5

docs: |2-
   # Create Complaint

    Submits an employee complaint or issue report. Logged under the
    `Employee Complaint` DocType.

    ## Authentication
    Requires a valid Bearer token.

    ## Parameters

    | Name     | Type   | Required | Description                                    |
    |----------|--------|----------|------------------------------------------------|
    | employee | string | Yes      | Employee ID (e.g., `HR-EMP-00003`)            |
    | date     | date   | Yes      | Complaint date in `YYYY-MM-DD` format          |
    | message  | string | Yes      | Detailed description of the complaint or issue |

    ## Response

    ```json
    {
      "message": {
        "name": "EMP-COMP-00001",
        "employee": "HR-EMP-00003",
        "date": "2026-06-01",
        "status": "Open"
      }
    }
    ```

    ## Notes
    - Complaints are created in `Open` status and are reviewed by HR.
    - The `message` field supports plain text; no HTML or special formatting.
